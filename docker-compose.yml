version: '3.8'

services:
  # Web Server (Apache + PHP)
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: digiparc_web
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./:/var/www/html
      - ./storage:/var/www/html/storage
      - ./public/uploads:/var/www/html/public/uploads
    environment:
      - APP_ENV=production
      - DB_HOST=db
      - DB_NAME=digiparc
      - DB_USER=digiparc_user
      - DB_PASS=digiparc_password
    depends_on:
      - db
      - redis
    networks:
      - digiparc_network

  # Database (MySQL)
  db:
    image: mysql:8.0
    container_name: digiparc_db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: digiparc
      MYSQL_USER: digiparc_user
      MYSQL_PASSWORD: digiparc_password
      MYSQL_ROOT_PASSWORD: root_password
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
      - ./database/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
      - ./database/migrations:/docker-entrypoint-initdb.d/migrations
    networks:
      - digiparc_network

  # Redis (Cache & Sessions)
  redis:
    image: redis:7-alpine
    container_name: digiparc_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - digiparc_network

  # phpMyAdmin (Database Management)
  phpmyadmin:
    image: phpmyadmin:latest
    container_name: digiparc_phpmyadmin
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      PMA_HOST: db
      PMA_USER: digiparc_user
      PMA_PASSWORD: digiparc_password
    depends_on:
      - db
    networks:
      - digiparc_network

  # Backup Service (Automated Backups)
  backup:
    image: alpine:latest
    container_name: digiparc_backup
    restart: unless-stopped
    volumes:
      - ./storage/backups:/backups
      - db_data:/var/lib/mysql:ro
    command: >
      sh -c "
        apk add --no-cache mysql-client &&
        while true; do
          mysqldump -h db -u digiparc_user -pdigiparc_password digiparc | gzip > /backups/backup_$$(date +%Y%m%d_%H%M%S).sql.gz &&
          find /backups -name 'backup_*.sql.gz' -mtime +7 -delete &&
          sleep 86400;
        done
      "
    depends_on:
      - db
    networks:
      - digiparc_network

volumes:
  db_data:
    driver: local
  redis_data:
    driver: local

networks:
  digiparc_network:
    driver: bridge
